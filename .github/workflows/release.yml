name: Create Addon Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup GitHub CLI
        run: |
          # Устанавливаем GitHub CLI для управления релизами
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
          sudo apt update
          sudo apt install gh -y

      - name: Extract version from tag
        id: get_version
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Building version: $VERSION"

      - name: Clean up existing release
        run: |
          # Удаляем существующий релиз если есть
          gh release delete "${{ github.ref_name }}" --yes || true
          echo "✅ Cleaned up existing release if any"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create addon directory structure
        run: |
          mkdir -p AferistHelper
          cp AferistHelper.toc AferistHelper/
          cp AferistHelper.lua AferistHelper/
          cp MuteManager.lua AferistHelper/
          cp AferistHelper_Configs.lua AferistHelper/

      - name: Create ZIP archive
        run: |
          zip -r "AferistHelper-${{ steps.get_version.outputs.version }}.zip" AferistHelper/
          echo "📁 Created: AferistHelper-${{ steps.get_version.outputs.version }}.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: AferistHelper ${{ steps.get_version.outputs.version }}
          body: |
            ## AferistHelper ${{ steps.get_version.outputs.version }}
            
            ### 📦 Новый релиз
            
            **Скачать:** `AferistHelper-${{ steps.get_version.outputs.version }}.zip`
            
            **Установка:**
            1. Распакуйте архив по пути: `World of Warcraft/_classic_/Interface/AddOns/`
            2. Перезапустите игру или используйте команду /reload в чате
            
            
          files: |
            AferistHelper-${{ steps.get_version.outputs.version }}.zip
